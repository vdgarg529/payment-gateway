# Demo Payment Service - Environment Configuration
# Copy this file to .env and adjust values as needed

# ============================================
# MongoDB Configuration
# ============================================
MONGO_PORT=27017

# ============================================
# Backend Configuration
# ============================================
BACKEND_PORT=8000

# ============================================
# Frontend Configuration
# ============================================
FRONTEND_PORT=3000

# ============================================
# React App Environment (used during frontend build)
# ============================================
# Choose ONE of the following options:
#
# Option 1: Using nginx proxy (RECOMMENDED for production)
# Frontend calls /api/* which nginx proxies to backend container
# This is the default if not set
REACT_APP_API_URL=/api
#
# Option 2: Direct backend URL (for local development)
# REACT_APP_API_URL=http://localhost:8000
#
# Option 3: AWS EC2 Public IP (if not using nginx proxy)
# REACT_APP_API_URL=http://<YOUR_EC2_PUBLIC_IP>:8000
